# deletion_code

## Environment

- Python 3.x

  ```bash
  pip install pulp
  ```

  Or if pipenv environment is ready,

  ```bash
  pipenv install
  ```

## Usage

- Execute

  ```bash
  python arrangement_optimizar.py
  ```
